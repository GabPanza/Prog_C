#include <stdio.h>

void calcula_corrida(float dist, float *b1, float *b2);
{
    int i;
}

void main()
{
    int distancia;
    float *band1=2.50, *band2=3.00;
    printf("Digite quantos Km foram percorridos: ");
    scanf("%d", distancia);
    calcula_corrida(distancia, *band1, *band2);
}
